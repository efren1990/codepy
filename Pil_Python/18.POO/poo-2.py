#---------------------------------------------------#
#POO->Programacion Orientada a Objetos
#---------------------------------------------------#

#CONCEPTOS O TERMINOS DE LA POO

"""
1.CLASE->Modelo donde se redactan las caracteristicas comunes
de un grupo de objetos

2.EJEMPLAR DE CLASE(Instacia de clase, objeto de clase)->
Ejemplar perteneciente a una clase (es decir un objeto)

3.MODULARIZACION->En un programa o aplicacion compleja por lo general
esta compuesto por varias o muchas clases, las cuales son como modulos
del programa en general,similar a un equipo de sonido antiguo, los cuales
eran construidos por modulos(casstera, torna disco, diales, controles, radio etc...)
Esto quiere decir que si uno de los modulos falla, el programa no se caera por
completo ya que fallara solo el modulo en si y no la aplicacion en general.

4.ENCAPSULAMIENTO/ENCAPSULACION->El funcionamiento de un modulo de un programa
esta conectada con otroas para funcionar como equipo, pero ninguna sabe que hace 
una de la otra, es decir la clase esta encapsulada para que el funcionamiento interno
de esa clase no sea accesible desde afuera.
Para conectar una clase con otra se conocen como Metodos de Acceso
"""