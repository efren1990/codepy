"""
EN PYTHON
    Es un conjunto de instrucciones que se encuantran en el mismo
    nivel de identacion, es decir en el mismo nivel jerarquico.

"""
a = 25
if(True):
    a = 50
    if (False):
        b = 7;

"""Las variables declaradas dentro del bloque de instrucciones identadas
solo tendran uso y valor dentro del mismo bloque de codigo donde se genero"""
print(a)
print(b)